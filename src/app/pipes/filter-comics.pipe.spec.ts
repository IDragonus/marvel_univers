import { FilterComicsPipe } from './filter-comics.pipe';

describe('FilterComicsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterComicsPipe();
    expect(pipe).toBeTruthy();
  });
});
